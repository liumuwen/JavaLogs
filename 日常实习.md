## 20260121
* 滑动窗口均值
* 搜索匹配
* 智享食惠用了这么多redis不怕数据丢失吗
## 20260122
### 爱学习
* 自学还是系统学习
* jdk8 17重要特性
* List<Order> --> map<Long,Order>(Stream操作)-->如果long冗余，只要最开始一个，有什么API
* 项目中redis持久化是怎么选择的，有哪些参数
* 智享食惠的lua脚本是实现逻辑？为什么先判断库存，再判断一人一单
* 高并发有哪些操作，你用过哪些
* 分布式id为什么要加上时间戳，直接用自增序列不好吗
* 兑换码用bitmap校验是否兑换，考虑过批量查询性能吗
* threadlocal何时清除
* 你希望从实习中收获什么？
* map的get和contains的区别

### 虾皮二面

* 写题LeetCode 351 Android Unlock Patterns变题
  * 输入 起点，终点，点数
  * 输出 路径总数
  
  ![image-20260122182629124](D:\myself\工作\面经\assets\image-20260122182629124.png)
  
* 追问阻塞队列原理

## 20260126

### 小米一面

* 拷打智享食惠
* 索引类型（唯一索引和普通索引哪个插入效率高，跟buffer有关）-- 普通索引的效率高一点
* 项目不成熟，偏离生产，基础知识细节掌握不够



## 20260127

### 项目 && 八股

* 缓存三兄弟
* redis雪崩 宕机的情况如何解决
* redis cluster槽为什么这么多
* redis缓存击穿存空对象以后如果又有数据了呢？如何保证缓存的一致性
* redis用set做共同关注，如何解决大key，拆分以后一次变成查询多次如何解决
* 消息队列的使用场景
* redis为什么那么快
* 单体项目和微服务项目的区别
  * 单体
    * 优点 架构简单容易实现 代码集中，便于后期部署
    * 缺点 可用性低，容易雪崩；扩展成本高，为了提高某个子模块的性能不得不全量水平复制；
* 微服务项目需要哪些模块
  * 服务注册发现，配置中心，网关，分布式事务，sentinel

* 服务注册发现和配置中心的区别， 配置中心为什么不用spring config
  * spring config 基于git存配置，不断轮询poll检查数据是否更改
  * nacos长连接 server端主动推送给客户端 低延迟 高性能（不浪费资源）

* 网关会承接所有流量，挂了怎么办
  * 多实例部署，请求负载均衡到不同实例
  * 流量分离，静态请求用CDN节点处理

* 常见消息队列对比，低延迟不就意味着高吞吐量吗
  * rabbitmq低延迟，可靠稳定
  * kafak高吞吐量，适用大数据场景，比如说日志收集，实时数据计算
  * rocketmq 阿里开源，kafka改造而来，原生支持延迟，死信队列，适用于互联网比如说电商，分布式微服务等场景
* 介绍xxl-job，他是怎么调度的
  * xxl-job 调度和执行分离
  * 使用流程：部署调度中心，集成执行器到业务，编写调度任务，控制台填写调度策略（cron)
  * 调度触发： 时间轮而不是全表扫描任务，根据任务的触发时间把它映射到时间轮盘的不同区间，定时扫描轮盘取出里面对应的任务执行

* synchronized和reentrylock区别，synchronized实现原理

### 算法

删除重复的链表节点
