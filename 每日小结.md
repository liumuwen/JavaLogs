## 20251223

* 三次握手，四次挥手
* TCP 的可靠性（确认应答，超时重传，流量控制，拥塞控制）
* pipline 和 redis 事务
* myisam innodb（存储，锁，事务，外键）
* spring ioc(反射，di) aop(动态代理)

## 20251224

* jvm调优
   * -xms xmn xmx
   * xx:metaspacesize xx:metaspacesizemax xx:+heapdumponoutofmerryerror xx:newRation xx:survivorRation
* 静态代理
* b+树时间复杂度
* Redis扣减了库存，但是MySQL却没有扣减
  （死信队列？）
  死信队列如何实现定时：普通队列（orderQueue）的消息成为死信 → 转发到死信交换机（orderDLX） → 死信交换机通过路由键（orderDLX.order）将消息路由到死信队列 → 消费者消费死信队列，处理超时逻辑

## 20251225
* 零拷贝[!https://www.cnblogs.com/xiaolincoding/p/13719610.html]
* threadlocal内存泄露
* concurrentHashMap 线程安全
* hashmap put流程
* 基本数据类型和引用类型存放

## 20251227
* TLS协议
* 红黑树插入

## 20251229
* 父子线程threadlocal是否可以访问（不可）
* hashmap为什么扩容两倍（方便扩容后，高效获取数据的新数组下标）
  hash-->高低位异或混合-->与
* exception(程序可以捕获，io,sql)，error(oom,栈溢出)
* redis memcache(事务，持久性，主从复制，数据结构)

## 20251230
* springbean的生命周期
* 慢接口排查--ping,top,具体接口（explain，架构（缓存，主从复制，读写分离））
* GC日志 线程转储 堆转储
* JVM内存结构中方法区与元空间的关系
* G1垃圾回收器的工作机制与调优参数  GCroots
* 数据库索引下推优化原理

## 20260106
* 类的加载（加载，链接，初始化）
* redis消费组（多个消费者协同消费）
* aqs(变量，管理线程的队列，操作资源变量的方法 tryacquire tryrelease) 独占 共享

## 20260108
* 手撕多线程（wait notify 关键在于实现接口的方法）
* 最左匹配结合范围查询（<>会截断后面的匹配）
* 拒绝策略（默认使用抛出错误）
* 线程工厂（默认线程工厂，特权线程工厂（访问安全管理器控制的系统资源），自定义线程工厂（自定义属性和异常）
  
## 20260117
* CPU占用过高排查（https://www.cnblogs.com/gswoniu/p/19473060）top toph jstack
* OOM排查（https://www.cnblogs.com/gswoniu/p/19473060）jps+jmap
* JMM 规范 主内存/本地内存 多线程编程 程序移植性
* 项目中使用的索引（课程表，排行榜）

## 20260118
* 出现大量的time_waited （出现大量短链接--短改长，复用time_wait，减少time_wait）
* 计算bitmap占用内存（2^23为1MB）
  
## 20260119
* jdk25 Scoped Values替代ThreadLocal、紧凑对象头、构造函数增强、实例main方法
* epoll (1)红黑树+就绪链表 （2）事件驱动，通过回调函数
  
## 20260120
* redis热key (1)二级缓存 （2）多节点备份
* redolog undolog binglog(statement row mix)
* spring容器（对象工厂） 三级缓存（创建对象的工厂）
* 为什么需要三级缓存（为了处理动态代理的bean，通过三级缓存，spring能动态的向二级缓存中存放半成品原始对象还是）

## 20260121
* 职业规划
  ![alt text](image.png)
* 35岁危机（不断学习）
* 索引 和 分表（大数据量--索引的维护开销，磁盘IO仍然很多）
* stream 和 mq（消息丢失，消息积压）
* redis cluster 哨兵模式（监控，选举，通知） 
  
## 20260122
* spring springboot springmvc
* 目的： （1）熟悉规范的工作流程 （2）掌握实际生产开发所需要的基础知识和技术水平，培养应对复杂线上环境的能力


## 20260123
* 限流算法（计数器，漏桶算法，令牌桶算法）

## 20260125
* 如何在保证并发安全的同时提高QPS（多级缓存，流量分离）
* mvcc防不住幻读，举个例子说明
* 类的成员变量，静态变量，方法的局部变量，这三种变量在多线程条件下哪个是线程安全的
* 对象头（mark word记录锁状态标志 和 偏向线程ID）
* synchronized锁升级的过程（没有线程访问，一个线程初次访问，多个线程访问（自旋））
* http1.1 长连接（TCP）
* http2 头部压缩 二进制 多路复用 全双工通信
* http3 tcp-->QUIC协议

## 20260126
* jdk版本 
  * jdk 8  stream流,lambda表达式
  * jdk 17 密封类 文本块
  * jdk 21 虚拟线程（jvm监管，若干个运行在传统线程，提高java并发程度）
* 线程的创建方式（继承thread，实现callable（结合futureTask，可返回结果），实现runable，线程池）
* 线程池参数
* 线程池类型（固定，单，缓存，周期线程池）
* redis的序列化（key序列化成string,value通过jackson序列化成json） jdk自带序列化（数据体积大，反序列不校验数据是否安全）
* transformer结构（编码器-->编码层-->多头注意力、FFN 解码器-->解码层-->掩码多头注意力、多头注意力（q）FFN）每个模块输出归一化+残差
* 唯一索引 普通索引插入性能（普通索引因为buffer的存在延迟合并写）
* 面向对象编程（以对象为核心的编程思想，通过对象交互解决问题，三大核心特性（封装 把事务的共性属性，方法封装起来，屏蔽细节）
* 注解（注释机制，不参与运行，提供信息，本质继承了anatation接口，编译器将其编译成字节码文件，程序运行时通过反射动态解析使用，实现类是动态代理类）生命周期，作用范围